from sys import argv
# this imports the module "exists" which returns True if a file exists, based on its name. False if not. comes from os.path whatever that is
#from os.path import exists

script, from_file, to_file = argv

#Today Zed Shaw was a douchebag. I spent almost an hour trying to do a challenge that said "See how short you can make the script. I could make this one line long." After reading documentation over and over, checking stack overflow and failing I looked at his FAQ and it said "That ; depends ; on ; how ; you ; define ; one ; line ; of ; code."



#print "Copying from %s to %s" % (from_file, to_file)

#we could do these two on one line, how?
#in_file = open(from_file)
#indata = in_file.read()

#print "The input file is %d bytes long" % len(indata)

#print "Does the output file exist? %r" % exists(to_file)
#print "Ready, hit RETURN to continue, CTRL-C to abort."
#raw_input()

#out_file = open(to_file, 'w')
#out_file.write(indata)

#print "Alright, all done."

#out_file.close
#in_file.close